% Установите размер изображения
width = 1000;
height = 1000;

% Создайте матрицу для хранения значений пикселей
img = zeros(height, width);

% Заполните матрицу случайными значениями от 0 до 1
img(:) = rand(height * width, 1);

% Отобразите изображение
imshow(img);
